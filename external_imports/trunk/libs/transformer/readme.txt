The lib folder is included here because it is part of the transformer package -- so we pull it in using svn externals in the bungenieditor resource folder.
we have to duplicate the lib here because svn does not allow pulling in files via the externals method.
